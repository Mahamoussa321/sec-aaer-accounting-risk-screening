# Analysis scripts

`run_main_benchmark.py` and `make_main_figures.py` are copied from the verified working repository by the clean-update runner before the old tree is replaced. The remaining scripts are included directly in the clean package.
